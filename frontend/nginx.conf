server {
  listen 80;

  root /usr/share/nginx/html;
  index index.html;

  # react router handles the routing on the client, so any path that is not
  # a real file should just get index.html and let the app take over
  location / {
    try_files $uri /index.html;
  }
}
